import { INestApplicationContext } from '@nestjs/common';
import { Arguments, CommandModule } from 'yargs';
export declare type FindCommandModule = (app: INestApplicationContext, command: string) => Promise<CommandModule>;
export declare class CommandModuleTest {
    private readonly _app;
    private readonly _opts;
    constructor(_app: INestApplicationContext, _opts?: {
        findCommandModule?: FindCommandModule;
    });
    execute<Result = any>(command: string, args: Partial<Arguments>, exitCode?: number): Promise<Result>;
}
